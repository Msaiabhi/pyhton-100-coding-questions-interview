# 100+ Python challenging programming exercises for Python 3

## 1. Level description
### Level 1	Beginner 
Beginner means someone who has just gone through an introductory Python course. He can solve some problems with 1 or 2 Python classes or functions. Normally, the answers could directly be found in the textbooks.

### Level 2	Intermediate 
Intermediate means someone who has just learned Python, but already has a relatively strong programming background from before. He should be able to solve problems which may involve 3 or 3 Python classes or functions. The answers cannot be directly be found in the textbooks.

### Level 3	Advanced. 
He should use Python to solve more complex problem using more rich libraries functions and data structures and algorithms. He is supposed to solve the problem using several Python standard packages and advanced techniques.

----

## 2. Problem template

Question
Hints
Solution

----

## 3. Questions

### Question 1
Level 1

Question:
Write a program which will find all such numbers which are divisible by 7 but are not a multiple of 5, between 2000 and 3200 (both included).
The numbers obtained should be printed in a comma-separated sequence on a single line.

Hints: 
Consider use range(#begin, #end) method

Solution:
```python
l=[]
for i in range(2000, 3201):
    if (i%7==0) and (i%5!=0):
        l.append(str(i))

print(','.join(l))
```
### Question 2
Level 1 (Easy)

Question:
Write a program to accept a sequence of comma-separated numbers and generate a list and a tuple containing those numbers.

Hints:
Use split() and type casting.

Solution:
```python
values = input().split(',')
l = list(values)
t = tuple(values)

print(l)
print(t)
```

### Question 3
Level 1 (Easy)

Question:
Write a program to calculate the factorial of a given number.

Hints:
Use a loop or recursion.

Solution:
```python
n = int(input())
fact = 1

for i in range(1, n+1):
    fact *= i

print(fact)
```

### Question 4
Level 1 (Easy)

Question:
Write a program to create a dictionary that contains numbers (1–n) as keys and their squares as values.

Hints:
Use a dictionary and loop.

Solution:
```python
n = int(input())
d = {}

for i in range(1, n+1):
    d[i] = i*i

print(d)
```

### Question 5
Level 1 (Easy – Interview Favorite)

Question:
Write a program to check whether a given number is even or odd.

Hints:
Use modulo operator %.

Solution:
```python
n = int(input())

if n % 2 == 0:
    print("Even")
else:
    print("Odd")
```

### Question 6
Level 2 (Easy–Moderate)

Question:
Write a program to count the number of letters and digits in a given string.

Hints:
Use isalpha() and isdigit().

Solution:
```python
s = input()
letters = 0
digits = 0

for ch in s:
    if ch.isalpha():
        letters += 1
    elif ch.isdigit():
        digits += 1

print("Letters:", letters)
print("Digits:", digits)
```

### Question 7
Level 2 (Moderate – Interview Question)

Question:
Write a program to check whether a given string is a palindrome.

Hints:
Compare the string with its reverse.

Solution:
```python
s = input()

if s == s[::-1]:
    print("Palindrome")
else:
    print("Not Palindrome")
```

### Question 8
Level 2 (Moderate)

Question:
Write a program to remove duplicate values from a list.

Hints:
Use a set or loop.

Solution:
```python
l = list(map(int, input().split()))
unique = list(set(l))

print(unique)
```

### Question 9
Level 2 (Moderate – Interview Favorite)

Question:
Write a program to find the largest and smallest number in a list.

Hints:
Use built-in functions or loop.

Solution:
```python
l = list(map(int, input().split()))

print("Largest:", max(l))
print("Smallest:", min(l))
```

### Question 10
Level 3 (Moderate – Important Interview Question)

Question:
Write a program to find Fibonacci series up to n terms.

Hints:
Use iteration.

Solution:
```python
n = int(input())
a, b = 0, 1

for i in range(n):
    print(a, end=" ")
    a, b = b, a+b
```

### Question 11
Level 3 (Moderate – Interview Coding Question)

Question:
Write a program to check whether a number is prime.

Hints:
Check divisibility up to square root.

Solution:
```python
n = int(input())
flag = True

if n <= 1:
    flag = False
else:
    for i in range(2, int(n**0.5)+1):
        if n % i == 0:
            flag = False
            break

if flag:
    print("Prime")
else:
    print("Not Prime")
```
Question 12

Level: Easy

Question:
Write a program to find the maximum of three numbers.

Hints:
Use conditional statements.

Solution:
```python
a = int(input())
b = int(input())
c = int(input())

if a >= b and a >= c:
    print(a)
elif b >= a and b >= c:
    print(b)
else:
    print(c)
```
Question 13

Level: Easy

Question:
Write a program to check whether a number is positive, negative, or zero.

Hints:
Use if–elif–else.

Solution:
```python
n = int(input())

if n > 0:
    print("Positive")
elif n < 0:
    print("Negative")
else:
    print("Zero")
```

Question 14

Level: Easy–Moderate

Question:
Write a program to find the length of a string without using len().

Hints:
Use a loop.

Solution:
```python
s = input()
count = 0

for _ in s:
    count += 1

print(count)
```
Question 15

Level: Moderate (Interview Question)

Question:
Write a program to find the sum of digits of a number.

Hints:
Use loop and modulo.

Solution:
```python
n = int(input())
total = 0

while n > 0:
    total += n % 10
    n //= 10

print(total)
```
Question 16

Level: Moderate

Question:
Write a program to generate a list of even numbers up to n.

Hints:
Use loop and condition.

Solution:
```python
n = int(input())
evens = []

for i in range(1, n + 1):
    if i % 2 == 0:
        evens.append(i)

print(evens)
```
Question 17

Level: Moderate (Interview Favorite)

Question:
Write a program to check whether a number is an Armstrong number.

Hints:
Sum of digits raised to power of number of digits.

Solution:
```python
n = int(input())
temp = n
power = len(str(n))
total = 0

while temp > 0:
    digit = temp % 10
    total += digit ** power
    temp //= 10

if total == n:
    print("Armstrong Number")
else:
    print("Not an Armstrong Number")
```
Question 18

Level: Moderate

Question:
Write a program to find the common elements between two lists.

Hints:
Use set intersection.

Solution:
```python
l1 = list(map(int, input().split()))
l2 = list(map(int, input().split()))

common = list(set(l1) & set(l2))
print(common)
```
Question 19

Topic: Loops & Conditions

Question:
Write a program to print all numbers between 1 and 100 that are divisible by both 3 and 5.

Solution:
```python
for i in range(1, 101):
    if i % 3 == 0 and i % 5 == 0:
        print(i)
```
Question 20

Topic: Loops & Conditions

Question:
Write a program to find the count of prime numbers between 1 and n.

Solution:
```python
n = int(input())
count = 0

for num in range(2, n + 1):
    prime = True
    for i in range(2, int(num ** 0.5) + 1):
        if num % i == 0:
            prime = False
            break
    if prime:
        count += 1

print(count)
```
Question 21

Topic: Functions

Question:
Write a function to return the square and cube of a given number.

Solution:
```python
def square_cube(n):
    return n*n, n*n*n

num = int(input())
result = square_cube(num)
print(result)
```
Question 22

Topic: Functions (Interview Question)

Question:
Write a function to check whether a number is a palindrome.

Solution:
```python
def is_palindrome(n):
    return str(n) == str(n)[::-1]

num = int(input())
print(is_palindrome(num))
```
Question 23

Topic: Lists

Question:
Write a program to find all elements in a list greater than the average of the list.

Solution:
```python
l = list(map(int, input().split()))
avg = sum(l) / len(l)

result = []
for i in l:
    if i > avg:
        result.append(i)

print(result)
```
Question 24

Topic: Lists

Question:
Write a program to merge two lists and remove duplicates.

Solution:
```python
l1 = list(map(int, input().split()))
l2 = list(map(int, input().split()))

merged = list(set(l1 + l2))
print(merged)
```
Question 25

Topic: Tuples

Question:
Write a program to find the index of an element in a tuple.

Solution:
```python
t = tuple(map(int, input().split()))
x = int(input())

print(t.index(x))
```
Question 26

Topic: Dictionaries

Question:
Write a program to check whether a key exists in a dictionary.

Solution:
```python
d = {1: 'a', 2: 'b', 3: 'c'}
key = int(input())

if key in d:
    print("Key exists")
else:
    print("Key does not exist")
```
Question 27

Topic: Dictionaries

Question:
Write a program to find the sum of all values in a dictionary.

Solution:
```python
d = {1: 10, 2: 20, 3: 30}
total = 0

for value in d.values():
    total += value

print(total)
```
Question 28

Topic: Mixed (Interview Level)

Question:
Write a function to remove all even numbers from a list.

Solution:
```python
def remove_even(l):
    result = []
    for i in l:
        if i % 2 != 0:
            result.append(i)
    return result

l = list(map(int, input().split()))
print(remove_even(l))
```
Question 29

Level 1 (Beginner)

Question:
Write a program to count how many numbers between 1 and 100 are divisible by 9.

Hints:
Use a loop and modulo operator.

Solution:
```python
count = 0

for i in range(1, 101):
    if i % 9 == 0:
        count += 1

print(count)
```
Question 30

Level 1 (Beginner)

Question:
Write a program to reverse a number.

Hints:
Use modulo and integer division.

Solution:
```py
n = int(input())
rev = 0

while n > 0:
    rev = rev * 10 + n % 10
    n //= 10

print(rev)
```
Question 31

Level 1 (Easy)

Question:
Write a program to print the first 10 natural numbers using a loop.

Hints:
Use range().

Solution:
```py
for i in range(1, 11):
    print(i)
```
Question 32

Level 2 (Easy–Moderate)

Question:
Write a program to check whether a year is a leap year.

Hints:
A leap year is divisible by 4 but not 100, unless divisible by 400.

Solution:
```py
year = int(input())

if (year % 400 == 0) or (year % 4 == 0 and year % 100 != 0):
    print("Leap Year")
else:
    print("Not a Leap Year")
```
Question 33

Level 2 (Moderate)

Question:
Write a program to find the second largest element in a list.

Hints:
Sort the list or track two maximum values.

Solution:
```py
l = list(map(int, input().split()))
l = list(set(l))
l.sort()

print(l[-2])
```
Question 34

Level 2 (Moderate – Interview Question)

Question:
Write a program to check whether two strings are anagrams.

Hints:
Sort both strings and compare.

Solution:
```py
s1 = input()
s2 = input()

if sorted(s1) == sorted(s2):
    print("Anagram")
else:
    print("Not Anagram")
```
Question 35

Level 2 (Moderate)

Question:
Write a program to find the frequency of each element in a list.

Hints:
Use a dictionary.

Solution:
```py
l = list(map(int, input().split()))
freq = {}

for i in l:
    freq[i] = freq.get(i, 0) + 1

print(freq)
```
Question 36

Level 2 (Moderate)

Question:
Write a program to find all prime numbers between 1 and 100.

Hints:
Use nested loops.

Solution:
```py
for num in range(2, 101):
    prime = True
    for i in range(2, int(num ** 0.5) + 1):
        if num % i == 0:
            prime = False
            break
    if prime:
        print(num, end=" ")
```
Question 37

Level 2 (Moderate – Interview Favorite)

Question:
Write a function to return the count of uppercase and lowercase letters in a string.

Hints:
Use isupper() and islower().

Solution:
```py
def count_case(s):
    upper = 0
    lower = 0
    for ch in s:
        if ch.isupper():
            upper += 1
        elif ch.islower():
            lower += 1
    return upper, lower

s = input()
print(count_case(s))
```
Question 38

Level 3 (Moderate)

Question:
Write a program to flatten a nested list.

Hints:
Use loop and extend().

Solution:
```py
nested = [[1, 2], [3, 4], [5, 6]]
flat = []

for sub in nested:
    flat.extend(sub)

print(flat)
```
Question 39

Level 3 (Moderate – Interview Question)

Question:
Write a program to find the first non-repeating character in a string.

Hints:
Use dictionary for counting.

Solution:
```py
s = input()
freq = {}

for ch in s:
    freq[ch] = freq.get(ch, 0) + 1

for ch in s:
    if freq[ch] == 1:
        print(ch)
        break
```
Question 40

Level 3 (Moderate)

Question:
Write a function to generate all divisors of a given number.

Hints:
Check divisibility using a loop.

Solution:
```py
def divisors(n):
    result = []
    for i in range(1, n + 1):
        if n % i == 0:
            result.append(i)
    return result

n = int(input())
print(divisors(n))
```
Question 41

Topic: Tuples

Question:
Write a program to convert a list into a tuple and find the maximum value.

Solution:
```python
l = list(map(int, input().split()))
t = tuple(l)

print(max(t))
```
Question 42

Topic: Dictionaries

Question:
Write a program to create a dictionary with word counts from a sentence.

Solution:
```python
sentence = input().split()
d = {}

for word in sentence:
    d[word] = d.get(word, 0) + 1

print(d)
```
Question 43

Topic: Loops and Conditions

Question:
Write a program to print the multiplication table of a given number up to 10.

Solution:
```python
n = int(input())

for i in range(1, 11):
    print(n, "x", i, "=", n * i)
```
Question 44

Topic: Loops and Conditions

Question:
Write a program to find the factorial of a number using a while loop.

Solution:
```python
n = int(input())
fact = 1

while n > 0:
    fact *= n
    n -= 1

print(fact)
```
Question 45

Topic: Functions

Question:
Write a function to find the maximum element in a list.

Solution:
```python
def find_max(l):
    max_val = l[0]
    for i in l:
        if i > max_val:
            max_val = i
    return max_val

l = list(map(int, input().split()))
print(find_max(l))
```
Question 46

Topic: Functions

Question:
Write a function to count vowels in a given string.

Solution:
```python
def count_vowels(s):
    count = 0
    for ch in s.lower():
        if ch in 'aeiou':
            count += 1
    return count

s = input()
print(count_vowels(s))
```
Question 47

Topic: Lists

Question:
Write a program to rotate a list to the left by one position.

Solution:
```python
l = list(map(int, input().split()))

rotated = l[1:] + [l[0]]
print(rotated)
```
Question 48
Level 1

Question:
Write a program to simulate a simple login system.
The user should be allowed only 3 attempts to enter the correct username and password.

Hints:

Use while loop

Use input()

Use if-else

Solution:
```py
correct_user = "admin"
correct_pass = "python123"
attempts = 3

while attempts > 0:
    user = input("Enter username: ")
    pwd = input("Enter password: ")

    if user == correct_user and pwd == correct_pass:
        print("Login Successful")
        break
    else:
        attempts -= 1
        print("Invalid credentials. Attempts left:", attempts)

if attempts == 0:
    print("Account Locked")
```
Question 49
Level 1

Question:
Write a program to accept N student marks from the user and display the average, highest, and lowest marks.

Hints:

Use list

Use input() inside loop

Use sum(), max(), min()

Solution:
```py
n = int(input("Enter number of students: "))
marks = []

for i in range(n):
    m = int(input(f"Enter marks of student {i+1}: "))
    marks.append(m)

print("Average:", sum(marks) / n)
print("Highest:", max(marks))
print("Lowest:", min(marks))
```
Question 50
Level 1

Question:
Write a program to count the frequency of each word in a sentence entered by the user.

Hints:

Use split()

Use dictionary

Use input()

Solution:
```py
sentence = input("Enter a sentence: ")
words = sentence.split()

freq = {}
for word in words:
    freq[word] = freq.get(word, 0) + 1

print(freq)
```
Question 51
Level 2

Question:
Write a program to simulate a bank account system where the user can deposit and withdraw money.

Hints:

Use class

Use input()

Use methods

Solution:
```python
class BankAccount:
    def __init__(self, balance):
        self.balance = balance

    def deposit(self, amount):
        self.balance += amount

    def withdraw(self, amount):
        if amount <= self.balance:
            self.balance -= amount
        else:
            print("Insufficient Balance")

    def show_balance(self):
        print("Current Balance:", self.balance)

balance = int(input("Enter initial balance: "))
account = BankAccount(balance)

dep = int(input("Enter amount to deposit: "))
account.deposit(dep)

wd = int(input("Enter amount to withdraw: "))
account.withdraw(wd)

account.show_balance()
```
Question 52
Level 1

Question:
Write a program to validate an email address entered by the user.

Hints:

Use input()

Check for '@' and '.'

Solution:
```py
email = input("Enter email address: ")

if "@" in email and "." in email:
    print("Valid Email")
else:
    print("Invalid Email")
```
Question 53
Level 2

Question:
Write a program to calculate the total bill of a shopping cart.
The user should enter item names and prices.

Hints:

Use list of dictionaries

Use loop

Use input()

Solution:
```py
cart = []
n = int(input("Enter number of items: "))

for i in range(n):
    item = input("Enter item name: ")
    price = int(input("Enter item price: "))
    cart.append({"item": item, "price": price})

total = 0
for product in cart:
    total += product["price"]

print("Total Bill:", total)
```
Question 54
Level 2

Question:
Write a program to count the number of words in a file whose name is entered by the user.

Hints:

Use file handling

Use input()

Use split()

Solution:
```py
filename = input("Enter file name: ")

with open(filename, "r") as f:
    content = f.read()
    print("Word Count:", len(content.split()))
```
Question 55
Level 2

Question:
Write a program to check the strength of a password entered by the user.

Hints:

Use len()

Use any()

Use string methods

Solution:
```py
password = input("Enter password: ")

if len(password) < 8:
    print("Weak Password")
elif any(c.isdigit() for c in password) and any(c.isupper() for c in password):
    print("Strong Password")
else:
    print("Moderate Password")
```
Question 56
Level 2

Question:
Write a program to filter and display salaries greater than a given amount entered by the user.

Hints:

Use input()

Use filter()

Use lambda

Solution:
```py
n = int(input("Enter number of employees: "))
salaries = []

for i in range(n):
    sal = int(input(f"Enter salary of employee {i+1}: "))
    salaries.append(sal)

limit = int(input("Enter salary limit: "))

result = list(filter(lambda x: x > limit, salaries))
print("Filtered Salaries:", result)
```
Question 57
Level 2

Question:
Write a program to divide two numbers entered by the user and handle division by zero.

Hints:

Use input()

Use try-except

Solution:
```py
a = int(input("Enter first number: "))
b = int(input("Enter second number: "))

try:
    result = a / b
    print("Result:", result)
except ZeroDivisionError:
    print("Cannot divide by zero")
```
Question 58
Level 2
HARD
Question:
Write a program to find all unique elements from a list entered by the user while preserving the original order.

Hints:

Use list

Use input()

Use membership checking

Solution:
```py
n = int(input("Enter number of elements: "))
lst = []
unique = []

for i in range(n):
    val = input("Enter element: ")
    lst.append(val)

for item in lst:
    if item not in unique:
        unique.append(item)

print("Unique elements:", unique)
```
Question 59
Level 2

Question:
Write a program to reverse each word in a sentence entered by the user.

Hints:

Use split()

Use slicing

Use loop

Solution:
```py
sentence = input("Enter a sentence: ")
words = sentence.split()

result = []
for word in words:
    result.append(word[::-1])

print(" ".join(result))
```
Question 60
Level 2

Question:
Write a program to check whether two strings entered by the user are anagrams.

Hints:

Use sorting

Use input()

Ignore spaces

Solution:
```py
s1 = input("Enter first string: ").replace(" ", "")
s2 = input("Enter second string: ").replace(" ", "")

if sorted(s1) == sorted(s2):
    print("Anagrams")
else:
    print("Not Anagrams")
```
Question 61
Level 2

Question:
Write a program to generate a dictionary where keys are numbers from 1 to N and values are their squares.

Hints:

Use dictionary

Use loop

Use input()

Solution:
```py
n = int(input("Enter value of N: "))
result = {}

for i in range(1, n + 1):
    result[i] = i * i

print(result)
```
Question 62
Level 3 (HARD)

Question:
Write a program to simulate a Library Management System where the user can:

Add books

Borrow a book

Return a book

Each book should be stored with its availability status.

Hints:

Use dictionary

Use loop

Use conditional statements

Solution:
```py
library = {}

while True:
    print("\n1. Add Book\n2. Borrow Book\n3. Return Book\n4. Exit")
    choice = input("Enter choice: ")

    if choice == "1":
        book = input("Enter book name: ")
        library[book] = "Available"

    elif choice == "2":
        book = input("Enter book name to borrow: ")
        if book in library and library[book] == "Available":
            library[book] = "Borrowed"
            print("Book borrowed successfully")
        else:
            print("Book not available")

    elif choice == "3":
        book = input("Enter book name to return: ")
        if book in library:
            library[book] = "Available"
            print("Book returned successfully")

    elif choice == "4":
        break
```
Explanation (Hard Program):

Dictionary stores book name as key and status as value

Menu-driven loop allows continuous operations

Demonstrates real-world application + data structures

Question 63
Level 2

Question:
Write a program to find the second largest number in a list entered by the user.

Hints:

Use set()

Use sorting

Solution:
```py
n = int(input("Enter number of elements: "))
nums = []

for i in range(n):
    nums.append(int(input("Enter number: ")))

nums = list(set(nums))
nums.sort()

print("Second largest:", nums[-2])
```
Question 64
Level 3 (HARD)

Question:
Write a program to validate a password with the following rules:

Minimum 8 characters

At least one uppercase

At least one lowercase

At least one digit

At least one special character

Hints:

Use any()

Use string methods

Use conditions

Solution:
```py
password = input("Enter password: ")

special = "!@#$%^&*"

if (len(password) >= 8 and
    any(c.isupper() for c in password) and
    any(c.islower() for c in password) and
    any(c.isdigit() for c in password) and
    any(c in special for c in password)):
    print("Strong Password")
else:
    print("Invalid Password")
```
Explanation (Hard Program):

Uses multiple conditions combined logically

any() checks character existence efficiently

Mimics real authentication systems

Question 65
Level 3 (HARD)

Question:
Write a program to implement a simple ATM system with:

Balance inquiry

Deposit

Withdrawal

Exit option

The program should run continuously until the user exits.

Hints:

Use loop

Use functions

Use conditional statements

Solution:
```py
balance = int(input("Enter initial balance: "))

while True:
    print("\n1. Balance\n2. Deposit\n3. Withdraw\n4. Exit")
    choice = input("Enter choice: ")

    if choice == "1":
        print("Balance:", balance)

    elif choice == "2":
        amount = int(input("Enter deposit amount: "))
        balance += amount

    elif choice == "3":
        amount = int(input("Enter withdraw amount: "))
        if amount <= balance:
            balance -= amount
        else:
            print("Insufficient balance")

    elif choice == "4":
        print("Thank you")
        break
```
Explanation (Hard Program):

Menu-driven system

Real-world financial logic

Combines loops + conditions + user input

Question 66
Level 2

Question:
Write a program to find common elements between two lists entered by the user.

Hints:

Use sets

Use input()

Solution:
```py
n1 = int(input("Enter size of first list: "))
list1 = [input("Enter element: ") for _ in range(n1)]

n2 = int(input("Enter size of second list: "))
list2 = [input("Enter element: ") for _ in range(n2)]

common = list(set(list1) & set(list2))
print("Common elements:", common)
```
Question 67
Level 2

Question:
Write a program to count vowels and consonants in a string entered by the user.

Hints:

Use loop

Use string methods

Solution:
```py
text = input("Enter a string: ").lower()
vowels = "aeiou"

v = c = 0
for ch in text:
    if ch.isalpha():
        if ch in vowels:
            v += 1
        else:
            c += 1

print("Vowels:", v)
print("Consonants:", c)
```
Question 68
Level 2

Question:
Write a program to print a list after removing all duplicate values while preserving the original order.

Hints:

Use set() to track seen elements

Maintain a new list for order

Solution:

def removeDuplicate(li):
    newli = []
    seen = set()

    for item in li:
        if item not in seen:
            seen.add(item)
            newli.append(item)

    return newli

li = list(map(int, input("Enter list elements separated by space: ").split()))
print(removeDuplicate(li))
Question 69
Level 2

Question:
Define a class Person and its two child classes Male and Female.
Each class should have a method getGender() that prints the gender.

Hints:

Use inheritance

Override methods in child classes

Solution:

class Person:
    def getGender(self):
        return "Unknown"

class Male(Person):
    def getGender(self):
        return "Male"

class Female(Person):
    def getGender(self):
        return "Female"

m = Male()
f = Female()

print(m.getGender())
print(f.getGender())
Question 70
Level 2

Question:
Write a program that counts and prints the number of occurrences of each character in a string entered by the user.

Example Input:

abcdefgabc
Example Output:

a,2
b,2
c,2
d,1
e,1
f,1
g,1
Hints:

Use dictionary

Use dict.get()

Solution:

s = input("Enter a string: ")
dic = {}

for ch in s:
    dic[ch] = dic.get(ch, 0) + 1

for k, v in dic.items():
    print(f"{k},{v}")
Question 71
Level 1

Question:
Write a program which accepts a string from the user and prints it in reverse order.

Example Input:

rise to vote sir
Example Output:

ris etov ot esir
Hints:

Use slicing [::-1]

Solution:

s = input("Enter a string: ")
print(s[::-1])
Question 72
Level 1

Question:
Write a program which accepts a string and prints characters present at even indexes.

Example Input:

H1e2l3l4o5w6o7r8l9d
Example Output:

Helloworld
Hints:

Use slicing with step size

Solution:

s = input("Enter a string: ")
print(s[::2])
Question 73
Level 2

Question:
Write a program to print all permutations of a list entered by the user.

Hints:

Use itertools.permutations()

Solution:

import itertools

li = list(map(int, input("Enter elements separated by space: ").split()))
perms = itertools.permutations(li)

print(list(perms))
Question 74
Level 3

Question:
Write a program to solve the classic puzzle:
There are 35 heads and 94 legs among chickens and rabbits.
Find the number of chickens and rabbits.

Hints:

Chicken has 2 legs

Rabbit has 4 legs

Use loop to try combinations

Solution:

heads = int(input("Enter number of heads: "))
legs = int(input("Enter number of legs: "))

found = False
for chickens in range(heads + 1):
    rabbits = heads - chickens
    if chickens * 2 + rabbits * 4 == legs:
        print("Chickens:", chickens)
        print("Rabbits:", rabbits)
        found = True
        break

if not found:
    print("No solution")
🔹 NEW QUESTIONS (Same Type, Added)
Question 75
Level 2

Question:
Write a program to find the first non-repeating character in a string.

Hints:

Use dictionary

Count frequency first

Solution:

s = input("Enter a string: ")
freq = {}

for ch in s:
    freq[ch] = freq.get(ch, 0) + 1

for ch in s:
    if freq[ch] == 1:
        print("First non-repeating character:", ch)
        break
Question 76
Level 2

Question:
Write a program to check whether a given list is a palindrome.

Hints:

Compare list with its reverse

Solution:

li = input("Enter list elements: ").split()

if li == li[::-1]:
    print("Palindrome List")
else:
    print("Not a Palindrome List")
Question 77
Level 3 (Hard)

Question:
Write a program to group words by their length from a sentence entered by the user.

Hints:

Use dictionary

Length as key

Solution:

sentence = input("Enter a sentence: ")
words = sentence.split()

result = {}
for word in words:
    result.setdefault(len(word), []).append(word)

print(result)
Explanation (Hard):

Dictionary keys represent word length

setdefault() avoids key checking

Useful in text analysis and NLP preprocessing
Question 78
Puzzle

Question:
We count 35 heads and 94 legs among the chickens and rabbits in a farm.
How many chickens and how many rabbits are there?

Hint:

Chicken has 2 legs

Rabbit has 4 legs

Use a for loop to try all possible combinations

Solution:

def solve(num_heads, num_legs):
    for chickens in range(num_heads + 1):
        rabbits = num_heads - chickens
        if chickens * 2 + rabbits * 4 == num_legs:
            return chickens, rabbits
    return "No solution"

heads = 35
legs = 94
print(solve(heads, legs))
Question 79
Puzzle

Question:
A shopkeeper buys an item for ₹800 and sells it at a profit of 25%.
Write a program to calculate the selling price.

Hint:

Selling Price = Cost Price + Profit

Profit = (Profit % × Cost Price)

Solution:

cost_price = int(input("Enter cost price: "))
profit_percent = int(input("Enter profit percentage: "))

profit = (profit_percent / 100) * cost_price
selling_price = cost_price + profit

print("Selling Price:", selling_price)
Question 80
Puzzle

Question:
A number is divisible by both 3 and 5 but not divisible by 10.
Write a program to find all such numbers between 1 and N.

Hint:

Use modulus operator %

Use loop

Solution:

n = int(input("Enter value of N: "))

result = []
for i in range(1, n + 1):
    if i % 3 == 0 and i % 5 == 0 and i % 10 != 0:
        result.append(i)

print(result)
Question 81
String Logic

Question:
Please write a program which accepts a string from console and prints it in reverse order.

Example Input:

rise to vote sir
Example Output:

ris etov ot esir
Hint:

Use slicing [::-1]

Solution:

s = input("Enter a string: ")
print(s[::-1])
Question 82
Puzzle | Interview Logic

Question:
Write a program to check whether a number entered by the user is a Perfect Number.
(A perfect number is a number whose sum of proper divisors equals the number.)

Example:
6 → 1 + 2 + 3 = 6 ✔

Hint:

Use loop

Exclude the number itself

Sum divisors

Solution:

n = int(input("Enter a number: "))
total = 0

for i in range(1, n):
    if n % i == 0:
        total += i

if total == n:
    print("Perfect Number")
else:
    print("Not a Perfect Number")
Question 83
Puzzle | Interview Logic

Question:
Write a program to find the missing number in a sequence of numbers from 1 to N.

Example Input:

1 2 4 5
Output:

Missing number is 3
Hint:

Use sum formula: n*(n+1)//2

Subtract actual sum

Solution:

nums = list(map(int, input("Enter numbers: ").split()))
n = len(nums) + 1

expected_sum = n * (n + 1) // 2
actual_sum = sum(nums)

print("Missing number:", expected_sum - actual_sum)
Question 84
Puzzle | Interview Logic

Question:
Write a program to check whether a number is a power of 2.

Hint:

Use loop or bitwise logic

Keep dividing by 2

Solution:

n = int(input("Enter a number: "))

if n > 0 and (n & (n - 1)) == 0:
    print("Power of 2")
else:
    print("Not a Power of 2")
Question 85
Puzzle | Reasoning

Question:
Write a program to find the first repeating element in a list entered by the user.

Example Input:

10 5 3 4 3 5 6
Output:

First repeating element: 3
Hint:

Use set

Traverse list once

Solution:

nums = list(map(int, input("Enter numbers: ").split()))
seen = set()

for num in nums:
    if num in seen:
        print("First repeating element:", num)
        break
    seen.add(num)
Question 86
Puzzle | Interview Logic

Question:
Write a program to check whether two numbers are co-prime.
(Two numbers are co-prime if their GCD is 1.)

Hint:

Use Euclidean algorithm

Compute GCD

Solution:

a = int(input("Enter first number: "))
b = int(input("Enter second number: "))

x, y = a, b
while y != 0:
    x, y = y, x % y

if x == 1:
    print("Co-prime numbers")
else:
    print("Not co-prime")
Question 87
Puzzle | Interview Logic

Question:
Write a program to simulate a traffic signal system:

Red → Stop

Yellow → Ready

Green → Go

The user enters the signal color.

Hint:

Use dictionary or if-else

Normalize input

Solution:

signal = input("Enter signal color: ").lower()

if signal == "red":
    print("Stop")
elif signal == "yellow":
    print("Ready")
elif signal == "green":
    print("Go")
else:
    print("Invalid signal")

Question 88
Interview Logic

Question:
Write a program to check whether a number entered by the user is a Harshad (Niven) Number.
(A number is Harshad if it is divisible by the sum of its digits.)

Hint:

Extract digits

Sum digits

Check divisibility

Solution:

n = int(input("Enter a number: "))
s = sum(int(d) for d in str(n))

if n % s == 0:
    print("Harshad Number")
else:
    print("Not a Harshad Number")
Question 89
Reasoning Puzzle

Question:
Write a program to rotate a list to the right by K positions.

Hint:

Use slicing

Use modulo operator

Solution:

li = list(map(int, input("Enter list elements: ").split()))
k = int(input("Enter rotation count: "))

k = k % len(li)
rotated = li[-k:] + li[:-k]

print(rotated)
Question 90
Interview Logic

Question:
Write a program to check whether a number is an Armstrong Number.
(Example: 153 → 1³ + 5³ + 3³ = 153)

Hint:

Convert number to string

Use power operator

Solution:

n = input("Enter a number: ")
power = len(n)

total = sum(int(d)**power for d in n)

if total == int(n):
    print("Armstrong Number")
else:
    print("Not an Armstrong Number")
Question 91
Reasoning Puzzle

Question:
Write a program to find the longest word in a sentence entered by the user.

Hint:

Use split()

Use max() with key

Solution:

sentence = input("Enter a sentence: ")
words = sentence.split()

longest = max(words, key=len)
print("Longest word:", longest)
Question 92
Interview Logic

Question:
Write a program to check whether a given year is a leap year.

Hint:

Divisible by 4

Century years divisible by 400

Solution:

year = int(input("Enter year: "))

if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0):
    print("Leap Year")
else:
    print("Not a Leap Year")
Question 93
Reasoning Puzzle

Question:
Write a program to find the count of prime numbers between 1 and N.

Hint:

Nested loop

Check divisibility

Solution:

n = int(input("Enter value of N: "))
count = 0

for num in range(2, n + 1):
    prime = True
    for i in range(2, int(num**0.5) + 1):
        if num % i == 0:
            prime = False
            break
    if prime:
        count += 1

print("Prime count:", count)
Question 94
Interview Logic

Question:
Write a program to check whether two strings entered by the user are rotations of each other.

Example:
ABCD and CDAB → Yes

Hint:

Concatenate first string with itself

Use in operator

Solution:

s1 = input("Enter first string: ")
s2 = input("Enter second string: ")

if len(s1) == len(s2) and s2 in s1 + s1:
    print("Rotation of each other")
else:
    print("Not a rotation")
Question 95
Reasoning Puzzle

Question:
Write a program to find the majority element in a list
(An element appearing more than n/2 times).

Hint:

Use dictionary

Count occurrences

Solution:

nums = list(map(int, input("Enter numbers: ").split()))
freq = {}

for n in nums:
    freq[n] = freq.get(n, 0) + 1

for k, v in freq.items():
    if v > len(nums) // 2:
        print("Majority element:", k)
        break
Question 96
Interview Logic

Question:
Write a program to convert a decimal number to binary without using built-in functions.

Hint:

Use division by 2

Store remainders

Solution:

n = int(input("Enter a number: "))
binary = ""

while n > 0:
    binary = str(n % 2) + binary
    n //= 2

print("Binary:", binary)
Question 97
Reasoning Puzzle

Question:
Write a program to check whether parentheses in a string are balanced.

Example:
()()(( )) → Balanced

Hint:

Use stack (list)

Push and pop

Solution:

s = input("Enter parentheses string: ")
stack = []

for ch in s:
    if ch == "(":
        stack.append(ch)
    elif ch == ")":
        if not stack:
            print("Not Balanced")
            break
        stack.pop()
else:
    if not stack:
        print("Balanced")
    else:
        print("Not Balanced")

